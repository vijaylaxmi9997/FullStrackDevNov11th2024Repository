spring:
  application:
    name: springboot-department-docker
  datasource:
    url: jdbc:mysql://localhost:3309/deptdb
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 9090

